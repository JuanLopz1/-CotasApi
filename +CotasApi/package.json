{
  "name": "+cotas-root",
  "private": true,
  "description": "Proxies npm scripts to ./frontend (where dependencies live). Run npm install in frontend once.",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
